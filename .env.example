# Port to expose on the host machine (default: 7860)
PORT=7860

# Optional: Cloudflare Tunnel Token for custom domains.
# If left empty, Cloudflare will automatically launch a free Quick Tunnel (*.trycloudflare.com).
TUNNEL_TOKEN=
